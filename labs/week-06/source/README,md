# LAB06 Source

ซอร์สโค้ดหลักของ LAB06 อยู่ในโฟลเดอร์ `../src/`

เนื่องจาก LAB06 ใช้โครงสร้างโปรเจกต์ตาม Course Starter ของ Express API
ซึ่งกำหนดให้ซอร์สโค้ดอยู่ที่:

`labs/week-06/src/`

ดังนั้นโฟลเดอร์ `source/` นี้มีไว้เพื่อให้สอดคล้องกับโครงสร้างมาตรฐาน
ของ Repository และระบบตรวจสอบ `verify:lab`

ซอร์สโค้ดที่ใช้จริงในการพัฒนาและทดสอบ LAB06 ยังคงอยู่ในโฟลเดอร์ `src/`
ตามโครงสร้างที่อาจารย์กำหนด